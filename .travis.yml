language: java
install: ant deps  

git:
  depth: 1

jdk:
  - oraclejdk8
  - oraclejdk9



# Default build script is "ant test".
# You can run many commands, one per line prefixed with "  - ".

script:
  - ant test